# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its databases focus without claiming live deployment or external usage.

## Cases

- `baseline`: `index fit`, score 145, lane `ship`
- `stress`: `join width`, score 169, lane `ship`
- `edge`: `constraint risk`, score 179, lane `ship`
- `recovery`: `plan drift`, score 131, lane `watch`
- `stale`: `index fit`, score 289, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
